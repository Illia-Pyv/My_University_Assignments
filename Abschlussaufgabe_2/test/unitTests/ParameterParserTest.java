package unitTests;

import static org.junit.Assert.*;

import org.junit.Before;
import org.junit.Test;

import edu.kit.informatik.commands.ParameterParser;

/**
 * @author Illia Pyvovar
 * @version 1.0
 */
public class ParameterParserTest {

    ParameterParser parser;
    
    /**
     * 
     */
    @Before
    public void setUp() {
        
    }

    /**
     * 
     */
    @Test
    public void test() {
        /*parser = new ParameterParser("5,5,A,+,L,+,D,+,A0,*,D0,+,L,*,d,*,L,+,C0,d,B0,+,C,+,L,+,B");
        String all = "";
        String string = "";
        String numbers = "";
        for (String result : parser.getAllParameters()) {
            all += result + ", ";
        }
        System.out.println(all);
        for (String result : parser.getStringParameters()) {
            string += result + ", ";
        }
        System.out.println(string);
        for (int result : parser.getNumberParameters()) {
            numbers += result + ", ";
        }
        System.out.println(numbers);*/
    }

}
